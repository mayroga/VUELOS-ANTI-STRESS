services:
  - type: web
    name: vuelos-anti-estres-api
    env: python
    buildCommand: "pip install -r requirements.txt && playwright install chromium"
    startCommand: "uvicorn main:app --host 0.0.0.0 --port $PORT"
    envVars:
      - key: STRIPE_API_KEY
        sync: false
